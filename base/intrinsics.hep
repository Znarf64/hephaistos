// This is purely for documentation

type_is_vector     :: proc($T: typeid) -> bool ---
type_is_float      :: proc($T: typeid) -> bool ---
type_is_bool       :: proc($T: typeid) -> bool ---
type_is_integer    :: proc($T: typeid) -> bool ---
type_is_numeric    :: proc($T: typeid) -> bool ---
type_is_complex    :: proc($T: typeid) -> bool ---
type_is_quaternion :: proc($T: typeid) -> bool ---
type_is_matrix     :: proc($T: typeid) -> bool ---

count_ones           :: proc(i: $I) where type_is_integer(I) || type_is_vector(I) -> I ---
count_zeros          :: proc(i: $I) where type_is_integer(I) || type_is_vector(I) -> I ---
count_leading_zeros  :: proc(i: $I) where type_is_integer(I) || type_is_vector(I) -> I ---
count_trailing_zeros :: proc(i: $I) where type_is_integer(I) || type_is_vector(I) -> I ---
count_leading_ones   :: proc(i: $I) where type_is_integer(I) || type_is_vector(I) -> I ---
count_trailing_ones  :: proc(i: $I) where type_is_integer(I) || type_is_vector(I) -> I ---
find_lsb             :: proc(i: $I) where type_is_integer(I) || type_is_vector(I) -> I ---
find_msb             :: proc(i: $I) where type_is_integer(I) || type_is_vector(I) -> I ---
reverse_bits         :: proc(i: $I) where type_is_integer(I) || type_is_vector(I) -> I ---

// synchronize workgroup control flow and memory
barrier :: proc() ---
